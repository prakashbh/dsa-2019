#include <stdio.h>
#include <stdlib.h>

int main()
{
    int array[5] = {2,3,4,6,8}
    int product = 0;
    int index = 0

    for (index = 0;index < 10; index++)
    {
        product = product * i[array];
    }

    printf("Array product is %f\n" product);
    return 0;
}
